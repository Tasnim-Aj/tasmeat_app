import 'package:flutter/material.dart';

class ListiningScreen extends StatelessWidget {
  const ListiningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
